/**
 * An empty class that serves as a placeholder or base class.
 */
class EmptyClass {
  // This class is intentionally left empty.
}

const emptyObject = new EmptyClass();
console.log(emptyObject);
